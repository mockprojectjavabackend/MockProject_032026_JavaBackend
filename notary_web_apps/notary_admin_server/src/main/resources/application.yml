spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: notary_admin_server

  datasource:
    url: jdbc:mysql://localhost:3306/notary_db
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080
  servlet:
    context-path: "/server"

cloudinary:
  cloud-name: dy7ovxa6z
  api-key: 883846163598763
  api-secret: i2xyh--p7wgdqIJhOJW33DjYJNw
